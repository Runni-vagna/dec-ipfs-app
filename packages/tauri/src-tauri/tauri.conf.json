{
  "$schema": "https://schema.tauri.app/config/2",
  "productName": "CIDFeed",
  "version": "0.1.0",
  "identifier": "app.cidfeed.desktop",
  "build": {
    "beforeDevCommand": "sh -c 'if curl -sf http://127.0.0.1:5173 >/dev/null; then exit 0; fi; if pgrep -f \"vite --port 5173\" >/dev/null; then for i in $(seq 1 40); do curl -sf http://127.0.0.1:5173 >/dev/null && exit 0; sleep 0.25; done; fi; pnpm --filter @cidfeed/ui dev'",
    "beforeBuildCommand": "pnpm --filter @cidfeed/ui build",
    "devUrl": "http://localhost:5173",
    "frontendDist": "../../ui/dist"
  },
  "app": {
    "windows": [
      {
        "title": "CIDFeed",
        "width": 1320,
        "height": 860,
        "resizable": true,
        "fullscreen": false
      }
    ],
    "security": {
      "csp": null
    }
  },
  "bundle": {
    "active": false,
    "targets": "all"
  }
}
